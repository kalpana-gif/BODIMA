<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bodima">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home" />
        <activity android:name=".Registration" />
        <activity android:name=".ExpenseDashBoard" />
        <activity android:name=".ExpenseHistory" />
        <activity android:name=".UserProfile" />
        <activity android:name=".LoginActivity">
            <!--intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter-->
        </activity>
        <activity android:name=".AddHouse" android:parentActivityName=".Home" />
        <activity android:name=".EditHouseDetails"  android:parentActivityName=".Home"/>
        <activity android:name=".HouseDetails" android:parentActivityName=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>